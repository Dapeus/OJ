#include<iostream>
#define M 1000

using namespace std;

void division(int num[], int len)
{
    
}

int main()
{
    int len;
    while (cin>>len)
    {

        if(len == 0)
        {
            return 0;
        }
        int num[M];
        for(int i = 0; i < len; i++)
        {
            cin>>num[i];
        }
       
    }
    return 0;
}